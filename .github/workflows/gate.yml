# Normal pull-request Gate checks for this repository. The long-lived
# ci/self-probe pull request enters this caller after each PAT push.
name: gate

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review, converted_to_draft]
    paths-ignore:
      - '**.md'
      - 'docs/**'

permissions:
  actions: read
  contents: read
  pull-requests: write

jobs:
  gate:
    uses: zlxlabs/gate/.github/workflows/gate-v2.yml@33fbd932cf8cf89130dfc8f170320764267b4d8a
    with:
      tier: personal
      runner: self
      has_ui: false
    secrets:
      FEISHU_CI_WEBHOOK: ${{ secrets.FEISHU_CI_WEBHOOK }}
